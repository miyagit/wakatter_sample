# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: <%= ENV["DATABASE_USERNAME"] || "root" %>
  password: <%= ENV["DATABASE_PASSWORD"] || "" %>
  host: <%= ENV["DATABASE_HOST"] || "localhost" %>
  timeout: 5000

development:
  <<: *default
  database: wakatter_sample_development

test:
  <<: *default
  database: wakatter_sample_test

production:
  <<: *default
  database: <%= ENV["DATABASE_NAME"] || "wakatter_sample_production" %>
